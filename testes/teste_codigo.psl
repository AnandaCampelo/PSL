// Teste de blocos de código em PSL

titulo
    Teste de Blocos de Código em PSL

subtitulo
    Múltiplas Linguagens

codigo python
    def fibonacci(n):
        if n <= 1:
            return n
        return fibonacci(n-1) + fibonacci(n-2)
    
    print(fibonacci(10))

codigo c
    #include <stdio.h>
    
    int main() {
        printf("Hello from PSL!\n");
        return 0;
    }

codigo sql
    SELECT nome, idade
    FROM usuarios
    WHERE idade > 18
    ORDER BY nome;

